The FSBL needs to be modify in order to initialize the USB. That includes initializing the clock generator.<br/>
To Modify the FSBL:<br/>
Replace the default source files with the files from from <project_folder>/VCS-3_pub/zynqmp_fsbl_src.<br/>
