The FSBL needs to be modify in order to initialize the USB. That includes initializing the clock generator.
To Modify the FSBL:
Replace the default source files with the files from from <project_folder>/VCS-3_pub/zynqmp_fsbl_src.
